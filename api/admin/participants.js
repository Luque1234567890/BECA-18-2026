import { api, clean, json, query, requireAdmin } from '../_lib.js';
export default api(async request => { requireAdmin(request); const search = clean(new URL(request.url).searchParams.get('q'), 100); const rows = await query(`SELECT u.first_name,u.last_name,u.email,u.phone,s.plan,s.status,s.start_date,s.end_date,p.status AS payment_status FROM users u LEFT JOIN LATERAL (SELECT * FROM subscriptions WHERE user_id=u.id ORDER BY created_at DESC LIMIT 1) s ON true LEFT JOIN LATERAL (SELECT * FROM payments WHERE user_id=u.id ORDER BY created_at DESC LIMIT 1) p ON true WHERE ($1='' OR concat_ws(' ',u.first_name,u.last_name) ILIKE '%'||$1||'%' OR u.email ILIKE '%'||$1||'%' OR u.phone ILIKE '%'||$1||'%') ORDER BY u.created_at DESC LIMIT 200`, [search]); return json({ participants: rows.rows }); });

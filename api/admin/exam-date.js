import { api, assertSameOrigin, json, query, readJson, requireAdmin, requireMethod } from '../_lib.js';
export default api(async request => { const admin = requireAdmin(request); requireMethod(request, 'PUT'); assertSameOrigin(request); const { examDate } = await readJson(request); if (!/^\d{4}-\d{2}-\d{2}$/.test(String(examDate)) || Number.isNaN(Date.parse(examDate))) throw Object.assign(new Error('Fecha de examen inválida.'), { status: 422 }); await query("INSERT INTO app_settings (key,value,updated_at,updated_by) VALUES ('exam_date',$1,NOW(),$2) ON CONFLICT (key) DO UPDATE SET value=EXCLUDED.value,updated_at=NOW(),updated_by=EXCLUDED.updated_by", [examDate, admin.sub]); return json({ examDate }); });

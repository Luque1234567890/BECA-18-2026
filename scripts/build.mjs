import { access, readFile } from 'node:fs/promises'; const required=['db-schema.sql','db-set-provisional-exam-date.sql','api/_lib.js','api/auth/register.js','api/auth/login.js','api/payments/create.js','api/payments/webhook.js','api/payments/mock-approve.js','mi-cuenta.html','admin.html','preparacion.html'];for(const file of required)await access(file);const schema=await readFile('db-schema.sql','utf8');for(const table of ['users','profiles','subscriptions','payments'])if(!schema.includes(`CREATE TABLE ${table}`))throw Error(`Falta tabla ${table}`);console.log('Build de verificación correcto: rutas, migración y recursos presentes.');

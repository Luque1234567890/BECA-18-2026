import { access, readFile } from 'node:fs/promises'; const required=['db-schema.sql','db-set-provisional-exam-date.sql','api/index.js','server/_lib.js','server/auth/register.js','server/auth/login.js','server/payments/create.js','server/payments/webhook.js','server/payments/mock-approve.js','mi-cuenta.html','admin.html','preparacion.html'];for(const file of required)await access(file);const schema=await readFile('db-schema.sql','utf8');for(const table of ['users','profiles','subscriptions','payments'])if(!schema.includes(`CREATE TABLE ${table}`))throw Error(`Falta tabla ${table}`);console.log('Build de verificación correcto: rutas, migración y recursos presentes.');

const form=document.querySelector('#profile-form'),result=document.querySelector('#profile-result');form.addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(form));const notes=[];if(data.secondary==='yes')notes.push('La secundaria culminada es uno de los requisitos que debes verificar en la convocatoria oficial.');else notes.push('Primero revisa la condición de culminación de secundaria exigida por la convocatoria antes de postular.');if(data.performance==='top')notes.push('Tu rendimiento declarado es compatible con el criterio académico que suele aplicar a varias modalidades; verifica siempre tu modalidad específica.');else if(data.performance==='middle')notes.push('El rendimiento requerido cambia según la modalidad. Revisa las bases para confirmar si tu caso aplica.');else notes.push('Solicita o revisa tu constancia de rendimiento antes de iniciar una postulación.');notes.push(data.situation==='unknown'?'Revisa tu clasificación y la modalidad que podría corresponderte en la fuente oficial.':'Reúne los documentos que acrediten tu situación antes de la postulación.');result.innerHTML=`<p class="eyebrow">ORIENTACIÓN INICIAL</p><h2>Siguientes pasos</h2><ul>${notes.map(note=>`<li>${note}</li>`).join('')}</ul><p><a class="button primary" target="_blank" rel="noopener" href="https://www.pronabec.gob.pe/beca-18/">REVISAR CONVOCATORIA OFICIAL</a></p>`;result.classList.remove('hidden');result.scrollIntoView({behavior:'smooth'});});
